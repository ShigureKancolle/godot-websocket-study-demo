Q: 
状态同步游戏项目中，本地玩家移动被服务端"拉扯"。经澄清确认的具体症状：
- **持续移动中**：走一段就被明显周期性回拉一次
- **转向/变向时**：换方向瞬间会折回或滑一段旧方向
- 测试环境：本机/局域网，RTT < 20ms（排除网络延迟因素）

A:  KiMi-K3
1. **服务端 tick 漂移（周期性回拉主因）**：`web_server.py` 的 `_tick_loop` 用 `await asyncio.sleep(TICK_INTERVAL)` 控频，但 `tick_movement` 用固定 33.3ms 积分。Windows 上 asyncio 定时器粒度约 15.6ms，实际 tick 间隔约 47ms（约 21Hz），服务端真实移速约 213px/s，与客户端预测的 300px/s 存在约 87px/s 恒速漂移，每约 170ms 累积超过对账阈值触发一次回拉。与 RTT 无关，局域网必现。
2. **变向处理窗口未覆盖（变向折回次因）**：客户端变向立即按新方向预测；服务端 pending_inputs 要等下一个 tick 才处理新方向（最长约 47ms），期间按旧方向继续推进。Role 软对账回溯 `now - RTT` 只覆盖网络延迟，没覆盖"输入在 pending 里等 tick"的 0~TICK_INTERVAL 延迟。180° 变向瞬间分歧约 20~35px，超过 15px 阈值被误判断节而折回。