# 生存玩法 MVP 总方案

## 1. 背景与范围

本方案定义参考《吸血鬼幸存者》的联机生存主循环，目标是完成一局可开始、可战斗、可升级、可死亡结算并可清理重开的最小版本（PLAN-20260818-002）。保留现有房间共享模型：同一 `GameRoom` 内的玩家共同参与一个 Run。本文是待实现设计，不把未实现的消息、字段或类当作既有事实。

非目标：局外养成、装备系统、Boss、怪物词条、独立副本、匹配系统、复杂障碍物寻路，以及改变现有攻击方式。

## 2. 权威边界与核心循环

服务端 `GameRoom`/Run 是唯一状态权威，负责玩家、敌人、经验球、计时、伤害、升级、暂停和结算。客户端 `ClientStateMirror` 只读服务端快照/事件并负责输入请求、表现和 UI，不得自行改变游戏状态。

流程为：进入房间并创建 Run → 玩家移动和自动攻击 → 服务端持续按预算刷怪并结算命中/死亡 → 敌人掉经验球，经验球到达玩家后加经验 → 升级并产生三选一 → 难度随时间和等级增加 → 玩家死亡 → 全员死亡后结算 → 返回主界面并销毁 Run 状态。

## 3. 单人/多人升级暂停

以 Run 内“活跃且未死亡玩家数”判断模式。数量为 1 时，升级选择期间暂停整个 Run 的战斗 tick、刷怪、敌人 AI、经验球移动和伤害；数量大于等于 2 时不暂停，其他玩家继续行动，升级玩家的候选奖励由服务端按玩家维护待选队列，选择完成后应用并取下一组。暂停中的 Run 不接受中途加入，或由后续实现明确延迟加入。

## 4. 敌人持续实体与牵引返程

每个敌人保存索敌目标玩家 ID。首次发现目标后正常追击期间不随意切换。距离大于 `LEASH_RETURN_DISTANCE = 1500px` 时，实体不销毁、不重建、不复用，也不重置生命、难度属性、经验归属或统计；停止普通巡逻、追击和攻击决策，进入 `returning` 状态，以返程速度朝目标直线移动。距离小于 `LEASH_RESUME_DISTANCE = 1200px` 后恢复普通 AI 和原目标。

`returning` 状态不攻击。目标离开、死亡或失效时，改绑最近的活跃玩家；无有效玩家时冻结。这里“停止 AI”仅指停止普通战斗 AI，返程是服务端专用状态迁移。首版无障碍物地图采用直线返程，未来接入寻路，不以重新创建敌人解决拉脱。

## 5. 波次、数值与经验

波次每 30 秒递增，仅用于统计和 UI；刷怪使用持续生成预算，不要求清完上一波。新敌人生成时固化属性：`基础属性 × 时间倍率 × 玩家等级倍率`，不追溯修改场上敌人。生成间隔随时间缩短，具体值由配置确定。

敌人死亡才生成经验球；经验只有在经验球吸附并到达目标玩家时结算。每 15 秒额外给予当前等级数量的经验作为保底。升级经验曲线和四种 MVP 奖励均配置化：攻击力百分比、最大生命值、攻击范围百分比、防御固定值；候选应去重并由服务端决定。默认目标是在约 8–10 分钟、约 15 级附近形成必死压力，实际需联调校准。

## 6. 待新增的关键数据与协议

协议设计应新增但不预设具体编号：Run 状态/时间/波次/暂停模式；玩家状态（位置、生命、等级、经验、统计）；敌人状态（位置、生命、目标 ID、AI 状态、生成时固化属性）；经验球状态；升级候选、玩家选择和待选队列；死亡与结算数据；返回主界面/销毁 Run 的结果。

所有入站请求必须由服务端校验权限、方向和当前 Run 状态；客户端只接受并校验服务端方向的镜像与事件。协议变更须同步 `messages.json`、proto 和双方生成物，但本次仅记录设计，不修改它们。

## 7. 任务拆分与依赖

| 任务 | 范围 | 依赖 |
|---|---|---|
| 01 | Run 状态、消息语义、契约边界 | 无 |
| 02 | 服务端刷怪、牵引返程、经验、升级、结算 | 01 |
| 03 | 共享配置和数值曲线 | 01，可与 02 并行 |
| 04 | 客户端镜像、HUD、奖励和结算 UI | 01，可与 02/03 并行 |
| 05 | 联调、状态清理和端到端验收 | 02、03、04 |

详细任务见 `docs/vs-like/`。预计涉及 `server/game`、`server/net`、`server/proto`、`shared_config`、`client/Script/Net`、`client/Script/UI` 及对应文档；实际实现时遵循各模块文档和配置同步规则。

## 8. 风险与待定项

- 返程速度、触发/恢复阈值、生成预算和成长倍率需实测调参。
- 多人模式下升级者承受战斗压力，MVP 不承诺无敌帧或慢动作。
- 目标切换需冷却，避免多人场景中敌人抖动。
- 全员死亡才结束共享 Run；离开、掉线和中途加入的精确定义需在协议任务中落定。

## 9. 端到端验收

单人能完成开始、移动、击杀、经验吸收、升级暂停/选择、死亡、四项结算和返回主界面；双人中一人升级时另一人持续移动和战斗。敌人被拉到 1500px 外仍保留原实体和血量，返程到 1200px 内恢复追击，目标失效可改绑或冻结。重开时无残留实体、计时器、经验球、奖励队列或客户端镜像；协议、配置副本和生成物在实现阶段按项目规则同步。
