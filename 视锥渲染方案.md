# 敌人视野(视锥)渲染方案

> 状态：✅ 已实现（2025-06 按本方案完成编码，见文末「实现记录」）
> 关联配置：`shared_config/vision_config.json`（双端共享，改完跑 `tools/sync_config.py` 同步）
> 关联文档：`docs/server-proto.md`、`docs/server-game.md`、`docs/client-role.md`

## 一、需求背景与目标

给敌人实体渲染一个**玩家可见的半透明扇形视锥**（潜行玩法：玩家能看见敌人的警戒范围）。

核心诉求：
1. 视锥分两种形态——`normal`（常态，宽而近）与 `chase`（追逐态，窄而远）。
2. 两种形态的切换**必须跟随敌人的 AI 状态**（`ai_state`）：`chase` 状态用 chase 视野，其余状态用 normal 视野。
3. 视锥朝向跟随实体 `facing` 实时旋转。

## 二、现状分析（已核实）

### 2.1 服务端已有但未同步的部分

| 现状 | 位置 | 说明 |
|------|------|------|
| AI 状态机 | `server/game/ai/enemy_ai_machine.py` + `states/` | 状态：`patrol` / `chase` / `attack` / `look_around` |
| 视野几何判定 | `server/game/helper/ai_state_helper.py` | `find_nearest_entity_in_sight(..., vision_mode="normal"/"chase")`、`is_in_sight()` |
| 视野配置加载 | `server/config/config_loader.py` | `get_vision(mode)` 已存在，读 `vision_config.json` |
| 视野配置数据 | `shared_config/vision_config.json` | `normal`=半角30°、半径750px；`chase`=半角22.5°、半径1000px |

### 2.2 缺失的关键一环：`ai_state` 未同步给客户端

| 位置 | 是否有 `ai_state` 字段 |
|------|----------------------|
| `EntityInfo` dataclass（`server/game/game_room.py`） | ❌ 无 |
| proto `EntityInfo`（`server/proto/game.proto`） | ❌ 无 |
| 客户端 `ClientEntityInfo`（`client/Script/Net/EntityInfo.gd`） | ❌ 无 |

> ⚠️ **重要区分**：现有 proto `EntityInfo.state` 是**动画状态**（idle/run/attacking/hurt），
> **不是** AI 状态。动画状态里没有 `chase`，无法据此切换视野形态。
> 因此必须**新增 `ai_state` 字段**（AI 状态：patrol/chase/attack/look_around），两者是独立维度。
> 下文"根据 state 渲染视锥"中的 state 一律指 **`ai_state`**。

### 2.3 客户端视锥几何基础已具备

- `client/Script/game/collision.gd`：`Sector` 扇形定义 + 点/线在扇形内判定（与服务端对称）。
- `facing` 机制：弧度，`0=右、逆时针正`（Godot 标准），`PlayerVisual.update_facing()` 已有先例。

## 三、方案总览

采用项目成熟的"**全量带初始 + 增量实时更新**"模式（完全复刻 `PlayerFacing`）：

1. **协议**：`EntityInfo` 加 `ai_state` 字段（GameState 快照 / 新玩家加入带初始 AI 状态）+ 新增 `AiStateChanged` S2C 增量消息（AI 状态切换时实时广播，切换视野不能等低频快照）。
2. **服务端**：AI 状态切换处广播 `AiStateChanged`。
3. **客户端**：新增 `VisionFan.gd`（Polygon2D 组件），根据 `ai_state` 选 normal/chase 参数、按 `facing` 旋转顶点，半透明显示。

## 四、协议变更

### 4.1 `EntityInfo` 加字段

```proto
message EntityInfo {
  ...
  string ai_state = 11;   // AI 状态:patrol/chase/attack/look_around(只有敌人填,玩家/木桩留空)
}
```

### 4.2 新增 `AiStateChanged` 增量消息

```proto
// AI 状态变更(S2C 增量,只改 ai_state 不改坐标/朝向)
// 和 PlayerFacing 平行:AI 状态是独立维度,状态切换时实时广播
message AiStateChanged {
  string entity_id = 1;      // 实体ID
  string ai_state = 2;       // 新的 AI 状态
}
```

### 4.3 登记

- 在 `GameMessage` 的 `oneof` / 消息枚举里登记 `AiStateChanged`。
- 同步 `server/proto/messages.json` 消息契约（方向=S2C、类别=状态增量）。
- **双端 proto 源文件都要改**：`server/proto/game.proto` 与 `client/Script/proto/game.proto`（同一份源）。

## 五、服务端改动

```
server/
├── proto/game.proto          【修改】EntityInfo 加 ai_state + 新增 AiStateChanged + 消息枚举登记
├── proto/messages.json       【修改】登记 AiStateChanged 契约
├── compile_proto.py          【运行】重新生成 proto/generated/game_pb2.py
├── game/game_room.py         【修改】EntityInfo dataclass 加 ai_state 字段
└── game/enemy_mgr.py         【修改】AI 状态切换时广播 AiStateChanged
```

### 5.1 `game_room.py` — EntityInfo 加字段

```python
@dataclass
class EntityInfo:
    ...
    state: str = "idle"          # 动画状态(不变)
    ai_state: str = "idle"       # 新增:AI 状态 patrol/chase/attack/look_around(敌人默认 patrol)
```

> ⚠️ **必须同步改 proto 字段**：GameState 广播用 `dataclasses.asdict()` 转 dict 再
> `ParseDict`，**多出的未知字段会报错**（项目在 `KnockbackState` 注释里记录过此坑）。
> 所以 dataclass 加字段的同时必须给 proto `EntityInfo` 加同名 `ai_state` 字段。

### 5.2 `enemy_mgr.py` — AI 状态切换广播

参照项目既有钩子模式（`GameRoom._attack_trigger` / `_entity_spawn_hook`）：

- 给 `EnemyMgr`（或 `EnemyAIMachine`）加一个"AI 状态变更回调"（`ai_state_change_hook`）。
- 在 `EnemyAIMachine.change_state()` 里，状态**真正切换**时触发回调（`old != new` 才触发，避免重复广播）。
- `GameServer` 初始化时注册该回调，`await server.broadcast("AiStateChanged", {"entity_id": ..., "ai_state": ...})`。

> AI 状态切换是低频事件（patrol→chase→attack），**状态切换即广播**（实时 + 简单），
> 不走 `_dirty_entities` 的 tick 统一广播（那个是给高频位置同步用的）。

## 六、客户端改动

```
client/
├── Script/proto/game.proto       【修改】同一份源,同步改(与 server/proto/game.proto 一致)
├── Script/gdproto/game.gd        【重新生成】godobuf 从 proto 生成(生成命令待确认)
├── Script/Net/EntityInfo.gd      【修改】ClientEntityInfo 加 ai_state + from_dict 转换
├── Script/Net/StateMirror.gd     【修改】注册 game.AiStateChanged → _on_ai_state_changed
├── Script/game/ConfigLoader.gd   【修改】新增 VisionInfo + get_vision(mode)(双端对称)
├── Script/role/VisionFan.gd      【新增】Polygon2D 组件(核心渲染)
└── Script/role/Role.gd           【修改】_setup_enemy 挂载 VisionFan + on_entity_updated 转发
```

### 6.1 `EntityInfo.gd` — ClientEntityInfo 加字段

```gdscript
# AI 状态(patrol/chase/attack/look_around,只有敌人有;动画 state 是另一个维度)
var ai_state: String = "idle"
```

`from_dict()` 里同步转换：`ai_state = d.get("ai_state", "idle")`。

### 6.2 `StateMirror.gd` — 增量消息处理器（复刻 `_on_player_facing`）

```gdscript
# register_handlers 里注册
mb.onproto("game.AiStateChanged", _on_ai_state_changed)

# 复刻 _on_player_facing:只改 ai_state,不动 x/y/facing,复用 entity_updated 信号
func _on_ai_state_changed(data: Dictionary) -> void:
    var eid: String = data.get("entity_id", "")
    if eid == "":
        return
    var entity: ClientEntityInfo = _entities.get(eid)
    if entity == null:
        return  # 镜像里没这个实体:忽略,等全量快照修正(和 _on_player_move 一致)
    entity.ai_state = data.get("ai_state", "idle")
    entity_updated.emit(entity)
```

### 6.3 `ConfigLoader.gd` — VisionInfo + get_vision（双端对称）

```gdscript
class VisionInfo:
    var half_angle: float = 0.0   # 半角(弧度),loader 构造时从度转弧度
    var radius: float = 0.0

static func get_vision(mode: String) -> VisionInfo:
    # 读 res://config/vision_config.json 的 normal/chase 段
    # JSON 里 half_angle_deg 是度,构造时转弧度(和服务端 config_loader 一致)
```

### 6.4 `VisionFan.gd` — 视锥渲染组件（核心）

```gdscript
extends Polygon2D
# 组件职责:读 vision_config + 按 ai_state 切 normal/chase + 按 facing 旋转生成扇形顶点
# 纯显示,不做障碍物遮挡(后续扩展)

var _radius: float = 750.0
var _half_angle: float = deg_to_rad(30.0)
var _color: Color = Color(1, 0, 0, 0.25)   # 半透明红

func setup(ai_state: String) -> void:
    set_ai_state(ai_state)
    set_color(...)

func set_ai_state(ai_state: String) -> void:
    # chase → 用 chase 视野(窄而远),其余 → normal 视野(宽而近)
    var mode := "chase" if ai_state == "chase" else "normal"
    var vision := ConfigLoader.get_vision(mode)
    _radius = vision.radius
    _half_angle = vision.half_angle
    queue_redraw()

func set_facing(facing: float) -> void:
    rotation = facing   # 扇形整体绕原点旋转,facing=0 时朝右(Godot 标准)
```

**顶点生成逻辑**（`_draw` 或 `polygon` 更新）：
- 以 `facing` 为基准（通过节点 `rotation` 或顶点角度加 facing 实现），顶点从 `Vector2.ZERO` 开始，沿弧线到 `facing ± half_angle`。
- 分段数 `segments = 32`（边缘平滑），顶点 `Vector2.RIGHT.rotated(angle) * _radius`。
- 用 `@export` 暴露颜色等参数（Godot 4 用 `@export` setter 响应 Inspector 变化，**不要用 Godot 3 的 `_property_changed`**，项目是 Godot 4.6）。

### 6.5 `Role.gd` — 挂载与转发

```gdscript
# _setup_enemy 里挂载(和 PlayerVisual 一样 add_child 挂子节点)
var vision_fan = preload("res://Script/role/VisionFan.gd").new()
vision_fan.name = "VisionFan"
add_child(vision_fan)
vision_fan.setup(info.ai_state)

# on_entity_updated 里转发(现有代码已有 visual.update_facing,顺势转发)
vision_fan.set_facing(info.facing)
vision_fan.set_ai_state(info.ai_state)
```

## 七、实施顺序

1. 改双端 proto 源（`EntityInfo` 加字段 + `AiStateChanged` 消息 + 枚举登记）。
2. 跑 `compile_proto.py` 生成服务端 `_pb2.py`；用 godobuf 重新生成客户端 `gdproto/game.gd`（命令待确认）。
3. 服务端：`game_room.py` 加字段 → `enemy_mgr.py` 加广播钩子 → `GameServer` 注册广播。
4. 客户端：`EntityInfo.gd` → `StateMirror.gd` → `ConfigLoader.gd` → `VisionFan.gd` → `Role.gd`。
5. 验证：进游戏放敌人，观察巡逻/追逐时视锥形态切换、转向时视锥跟随。

## 八、待确认点（已全部确认）

- [x] 客户端 `gdproto/game.gd` 的生成命令：**不需要手动跑**——按开发指南，客户端只改 `client/Script/proto/game.proto`，`gdproto/game.gd` 由 Godot 的 godobuf 插件在编译环节自动生成（Godot 打开项目时重新编译即可）。
- [x] 广播机制已定：状态切换即广播（钩子模式，不走 dirty 集合 tick）——`EnemyAIMachine.change_state` 状态真正切换（old != new）时调钩子，`GameServer._on_ai_state_changed` 同步 `EntityInfo.ai_state` + 广播 `AiStateChanged`。
- [x] 视锥纯显示、不做障碍物遮挡（后续可扩展射线遮挡）。
- [x] `z_index` 层级：`VisionFan.z_index = 1`（显示在角色/地形之上，半透明）。

## 九、实现记录（编码完成后维护）

- 协议：`EntityInfo` 加 `ai_state`（tag=11）；新增 `AiStateChanged`（tag=20，S2C）；`messages.json` 登记（S2C/event/state_affecting=false）。
- 服务端：`game_room.py` EntityInfo 加 `ai_state` 字段；`enemy_ai_machine.py` change_state 加状态切换钩子（old != new 才触发）；`enemy_mgr.py` 加 `set_ai_state_change_hook` 注入所有状态机；`web_server.py` 注册钩子 → `_on_ai_state_changed`（同步 EntityInfo.ai_state + `_queue_broadcast`）。
- 客户端：`EntityInfo.gd` 加 `ai_state` + `from_dict`；`StateMirror.gd` 注册 `_on_ai_state_changed`（只改 ai_state，复用 entity_updated 信号）；`ConfigLoader.gd` 加 `VisionInfo` + `get_vision(mode)`（JSON half_angle_deg 转弧度）；新建 `VisionFan.gd`（Polygon2D，按 ai_state 切 normal/chase + rotation 跟随 facing）；`Role.gd` `_setup_enemy` 挂载 + `on_entity_updated` 转发。
- 验证：服务端冒烟测试通过（初始 patrol 广播 + chase/attack 切换广播 + 重入不广播 + GameState 快照带 ai_state + AiStateChanged 序列化）；客户端需 Godot 重新生成 gdproto 后在游戏内验证（巡逻/追逐时视锥形态切换、转向时视锥跟随）。
