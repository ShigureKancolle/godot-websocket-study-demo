# 开发指南

记录项目的开发规则与约定。改代码前先看这里,按规则执行,避免踩坑。

## AI 规划—审批—执行工作流

项目使用主对话规划者和专用执行代理分离的工作流：

1. 主对话模型只负责需求交流、只读调查和制定带 `PLAN-ID` 的方案，不直接修改文件。
2. 用户检查方案后，使用 `批准执行 <PLAN-ID>` 明确授权实施。
3. 主对话模型把完整方案交给项目级自定义代理 `approved_executor`。
4. 执行代理固定使用 `gpt-5.6-luna` 和 `medium` 推理强度，配置位于根目录 `.codex/agents/approved-executor.toml`。
5. 如果实施需要实质改变方案，执行代理返回 `CHANGE_REQUEST`；主对话模型重新讨论并生成新 `PLAN-ID`，再次批准后才能继续。
6. 完成后，主对话模型以只读方式审查 diff 和验证结果，再向开发者汇报。

完整的角色边界、审批条件、回流规则和完成条件以根目录 `AGENTS.md` 的「规划—审批—执行工作流」为准。打开项目时应以 `D:\work2\godot_demo` 作为工作区根目录，确保 Codex 能加载根目录的 `.codex` 配置和 `AGENTS.md`。

## 代码修改后的 Review 流程(必做)

**每次改完代码,必须生成 diff 对比给开发者 review,再提交。**

- 工具:`tools/gen_review_diff.ps1`(入口 `tools/生成diff对比.bat`,双击即可)
- 原理:项目是多个独立 git 仓库(server/client/docs/shared_config/tools),没有统一 git 根。
  脚本自动扫描各仓库的**全部未提交改动**(修改+新增),用 `git show HEAD:` 导出"改前"版本,
  生成 `temp/review_diff/对比.bat`,逐个调用 TortoiseSVN 自带的 **TortoiseMerge** 打开
  图形化对比窗口(左=改前,右=改后,红绿高亮,和小乌龟右键 diff 一样)。
- 用法:
  1. 双击 `tools/生成diff对比.bat`(或让 AI 改完代码后自动执行)
  2. 逐个关掉 TortoiseMerge 窗口,自动进入下一个文件
  3. 全新文件(无改前版本)最后统一列清单,手动打开查看
- 注意:脚本扫描的是**所有**未提交改动(含他人/AI 之外的存量改动),review 时自行区分。
- 改完脚本本身(gen_review_diff.ps1)后,重跑一次验证它能正常生成。

## Git 多仓库一键操作

项目是 5 个独立 git 仓库(server/client/docs/shared_config/tools),都指向同一个 GitHub 仓库的不同分支。
日常提交/拉取用一键脚本,避免逐个仓库手动操作:

- 工具:`tools/git_onekey.ps1`(入口 `tools/git一键操作.bat`,双击出菜单)
- 菜单:
  - `0` 生成 diff 对比 review(等于直接调 `生成diff对比.bat`)
  - `1` 一键拉取:5 仓库统一 `pull --rebase`(**用 rebase 取代 merge,不再产生 merge commit**;未配 upstream 的分支自动配置)
  - `2` 一键提交并推送:逐仓库显示改动清单 → 逐个输入提交信息(回车=跳过该仓库)→ 确认后统一 push(推送前先 pull --rebase)
  - `3` 退出
- 规则:
  - **提交前必须先 review**(选项 0 或 `生成diff对比.bat`),确认无误再提交
  - 提交信息按仓库语义单独写(工具/功能/文档别混成一句话)
  - 任一仓库失败(冲突等)脚本立即停下报错,手动处理后重跑,不要静默继续
  - `git add -A` 会提交该仓库全部改动(含 `.uid`/生成物),提交前看清单自行判断
- 命令行模式:`powershell -File tools\git_onekey.ps1 status`(看全部改动)/ `review` / `pull`
- 注意:docs 和 tools 仓库当前分支可能没配 upstream,脚本会自动 `branch --set-upstream-to`,首次会自动配置

## proto 编译与更新规则

proto 是双端通信契约的源头,双端流程不同,按各自规则走:

### 服务端
- 修改 proto 源文件 `server/proto/game.proto` 后,**必须运行编译脚本**:
  ```
  cd server
  compile_proto.bat
  ```
  实际执行的是 `py -3 proto\compile_proto.py`,用 `grpcio-tools` 编译,生成 `server/proto/generated/game_pb2.py`。
- 生成的 `game_pb2.py` **不要手动编辑**,一律通过编译脚本重新生成。

### 客户端
- 客户端只需要修改 proto 文件(`client/Script/proto/game.proto`),**不用考虑编译 proto**。
- 客户端代码(`gdproto/game.gd`,godobuf 生成)的更新由编译环节自动完成,客户端侧不需要跑任何编译命令。

### 共同约定
- proto 消息修改后,双端契约 `messages.json` 也要同步更新(见 `docs/server-proto.md` 的「契约共享机制」)。
- 消息 ID(tag)一旦分配不要改动,追加新消息用新的编号。
